import { Surface as RNPSurface } from "react-native-paper";
import { withStyleProps } from "../../../hocs";

export const Surface = withStyleProps(RNPSurface);

Surface.displayName = "SurfaceWithStyleProps";